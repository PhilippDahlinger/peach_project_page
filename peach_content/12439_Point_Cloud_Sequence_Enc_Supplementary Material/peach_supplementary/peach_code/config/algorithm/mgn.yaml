
defaults:
  - /network/pc_encoder@encoder: dummy
  - /network/mat_prop_head@mat_prop_head: mlp  # TODO change to dummy
  - /network/simulator@simulator: mgn
  - meta_aggregation: softmax_pool
  - evaluator: base_evaluator
  - optimizer: adamw
  - scheduler: standard


type: mesh_to_sim
one_step_training: True  # Flag to check training loop to handle one step training (MGN and GGNS)
check_val_every_n_epoch: ${trainer.check_val_every_n_epoch}
material_properties: False  # oracle material properties
mat_prop_loss_weight: 0.0 # no mat prop loss
checkpoint_metric: val_ml_loss


# dataset related configs
data_modalities: ['mesh']
pc_preprocessing: ${dataset.pc_preprocessing}



# @package _global_

defaults:
  - override /algorithm: ggns_sd
  - override /algorithm/optimizer: adamw
  - override /dataset: step_ggns_sd_v1


algorithm:
  input_mesh_noise: 0.002

wandb:
  enabled: True


dataset:
  train_dataset:
    save_to_disk: True
  eval_dataset:
    save_to_disk: True

exp_name: sd_ggns_caching

hydra:
  sweeper:
    grid_params:
      seed: 1001
#      seed:  1002, 1003, 1004


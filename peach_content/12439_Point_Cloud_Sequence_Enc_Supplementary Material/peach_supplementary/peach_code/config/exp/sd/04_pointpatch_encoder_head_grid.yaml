# @package _global_

defaults:
  - override /algorithm: pointpatch_encoder_mango_decoder
  - override /dataset: sd_v1


wandb:
  enabled: True

exp_name: 0403_sd_pointpatch_encoder_head_grid


hydra:
  sweeper:
    grid_params:
      algorithm.predict_sdf: True, False
      algorithm.predict_mat_prop: True, False

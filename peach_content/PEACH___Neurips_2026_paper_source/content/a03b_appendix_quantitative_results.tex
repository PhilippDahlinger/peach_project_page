\section{Additional Quantitative Results.} \label{app:quant}
We provide full rollout MSE as a function of context size for all four environments in Figure~\ref{fig:context_mse}. \gls{peach} consistently improves with more context trajectories and achieves performance competitive with the Oracle baselines, while outperforming all learned point cloud encoders across environments.
\input{figures/ml_loss_context_line_plots}

% We additionally provide the quantitative results for the auxiliary losses ablation and the out-of-distribution generalization results in Figure~\ref{fig:generalizations_ablations}.
